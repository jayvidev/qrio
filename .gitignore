# Env (docker-compose usa .env en la raíz)
.env
.env.local
docker-compose.override.yml

# OS
.DS_Store

# IDE settings
.vscode/*
!.vscode/settings.json

# Root orchestration
node_modules/
pnpm-lock.yaml
!apps/web/pnpm-lock.yaml
package-lock.json
yarn.lock
bun.lockb
bun.lock

# Claude
.claude
