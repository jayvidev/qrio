package com.qrio.shared.config.security;

/**
 * Firebase removido del proyecto. Esta clase queda como stub no funcional
 * para evitar referencias antiguas. No se debe usar.
 */
public final class FirebaseTokenVerifier {
    public FirebaseTokenVerifier() {
        throw new IllegalStateException("Firebase eliminado del backend");
    }

    public Object verify(String idToken) {
        throw new IllegalStateException("Firebase eliminado del backend");
    }
}
